#!/bin/bash

# Infinite loop
while true
do
    # Run the Python program
    python3 Get_Gradients_Smooth_Grad_V2.py

    # Optional: Add a delay if you want to wait before restarting
    # sleep 5

    # The loop will continue and restart the script after it finishes
done
